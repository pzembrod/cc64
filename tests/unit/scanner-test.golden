
testing word.
do if for int auto case
char else goto break while extern
return static switch default continue register

++ += + -- -= - *= * /= /* / %=
% &= && & |= || | ^= ^ != ! ==
= <<= << <= < >>= >> >= > ~ 
